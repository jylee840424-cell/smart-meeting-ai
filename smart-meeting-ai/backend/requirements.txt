# =================================================================
# 🏢 무한상사 AI 거버넌스 시스템 - Backend Dependencies
# =================================================================

# 1. Backend Core Engine (FastAPI 기반 고성능 API 서버)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0

# 2. Audio & Video Processing (영상 추출 및 오디오 전처리)
yt-dlp>=2023.11.16
pydub>=0.25.1                 # [핵심] OpenAI STT 25MB 제한 우회를 위한 오디오 분할/압축 처리

# 3. AI & LLM Orchestration (LangChain 및 OpenAI 연동)
openai>=1.14.0
langchain>=0.1.13
langchain-community>=0.0.29
langchain-openai>=0.1.1         # ChatOpenAI, OpenAIEmbeddings 전용
langchain-text-splitters>=0.2.0 # RAG를 위한 텍스트 청크 분할 전용
tiktoken>=0.6.0                 # OpenAI 토큰 비용 계산 및 분할 최적화

# 4. Vector Database (RAG 지식 검색 엔진용 로컬 DB)
chromadb==0.4.24              # [안전장치] 버전 업데이트로 인한 충돌 방지를 위해 버전 고정

# 5. API Utilities & Data Validation
python-multipart>=0.0.9       # 프론트엔드로부터 비디오 파일(Form Data)을 업로드 받기 위해 필수

# 6. Environment Variables (보안 환경변수 관리)
python-dotenv>=1.0.1          # .env 파일 로드 (API 키 보안)

# =================================================================
# ⚠️ [시스템 필수 설치 사항 - 팀원 공유용 주석]
# pydub 및 yt-dlp가 정상 작동하려면 OS(Windows/Mac)에 'ffmpeg'가 설치되어 있어야 합니다.
# Windows: choco install ffmpeg
# Mac: brew install ffmpeg
# =================================================================