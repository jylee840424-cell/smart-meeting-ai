# =================================================================
# 🏢 무한상사 AI 거버넌스 시스템 - Backend Dependencies
# =================================================================
# ⚠️ [파이썬 버전]: Python 3.10.x 환경에서 실행을 강력히 권장합니다. 
# ⚠️ [가상환경 정책]: 본 프로젝트는 팀 협의에 따라 최상단(Root)에 단일 가상환경(venv)을 구성하여 공통으로 사용합니다.

# 1. Core Data Science & Vector DB (의존성 충돌 방지를 위해 최우선 배치)
# [팀 트러블슈팅 반영] ChromaDB 호환성을 위해 Numpy 1.x 버전으로 강제 고정
numpy==1.26.4
chromadb==0.5.23
chroma-hnswlib==0.7.6
SQLAlchemy==2.0.48

# 2. Dependency Resolution (백엔드와 프론트엔드 최종 타협안)
# [최종 수정] Streamlit 및 최신 AI 라이브러리 간의 모든 충돌 해결을 위한 하드 락킹
protobuf==4.25.8
packaging==23.2
rich==13.9.4
tenacity==8.5.0

# 3. Backend Core Engine (FastAPI 기반 고성능 API 서버)
fastapi==0.135.1
uvicorn==0.41.0
starlette==0.52.1
pydantic==2.12.5
pydantic-settings==2.13.1      # [추가] Pydantic V2 기반 안전한 환경변수(.env) 관리
python-multipart==0.0.22       # 프론트엔드 비디오 파일 업로드용
aiofiles==25.1.0               # [추가] 대용량 비디오 파일 비동기(Async) 논블로킹 저장

# 4. Audio, Video & Heavy ML Processing (영상 추출 및 오디오 전처리)
yt-dlp==2026.3.3
pydub==0.25.1                  # OpenAI STT 25MB 용량 제한 우회를 위한 오디오 분할/압축 처리용
torch==2.10.0                  # 로컬 AI 연산 및 Whisper 가동 엔진
openai-whisper==20250625       # 오디오 텍스트 변환(STT) 모델
onnxruntime==1.23.2

# 5. AI & LLM Orchestration (LangChain 및 AI 에이전트 연동)
openai==1.109.1
langchain==0.2.17
langchain-core==0.2.43
langchain-community==0.2.19
langchain-openai==0.1.25       # ChatOpenAI, OpenAIEmbeddings 전용
langchain-chroma==0.1.4        # 최신 ChromaDB 연동 모듈
langchain-text-splitters==0.2.4
tiktoken==0.12.0               # OpenAI 토큰 비용 계산 및 청크 분할 최적화
langgraph==1.1.0               # [추가] AI 자율 리포트 생성 에이전트 워크플로우 구축용
langgraph-checkpoint==4.0.1
langgraph-sdk==0.3.11
duckduckgo-search==8.1.1       # [추가] AI 에이전트 자율 웹 리서치용 무료 검색 도구

# 6. Telemetry & Observability (로깅 및 모니터링 경고 방지용)
opentelemetry-api==1.40.0
opentelemetry-sdk==1.40.0
opentelemetry-proto==1.40.0
opentelemetry-semantic-conventions==0.61b0

# =================================================================
# 🛠️ [시스템 필수 설치 사항 - OS 레벨]
# pydub 및 yt-dlp가 정상 작동하려면 PC(OS) 자체에 'FFmpeg'가 설치되어 있어야 합니다.
# 🪟 Windows (cmd/PowerShell 무관): winget install ffmpeg
# ⚠️ [주의]: FFmpeg 설치 완료 후 열려있는 터미널과 VS Code를 모두 재시작해야 적용됩니다.
# =================================================================